# === Global Settings ===
MAIN_DOMAIN="bookaistudio.com"
API_DOMAIN="api.${MAIN_DOMAIN}"
MAIL_DOMAIN="mail.${MAIN_DOMAIN}"
MAUTIC_DOMAIN="mautic.${MAIN_DOMAIN}"
N8N_DOMAIN="api.${MAIN_DOMAIN}"        # we proxy n8n at / on api.
POSTIZ_DOMAIN="postiz.${MAIN_DOMAIN}"   # change if you want

VPS_IP="168.231.74.188"
TIMEZONE="America/New_York"
ADMIN_EMAIL="admin@${MAIN_DOMAIN}"      # for Certbot

# === n8n Basic Auth ===
N8N_BASIC_USER="admin"
N8N_BASIC_PASS="ChangeMe!Strong1"

# === Mautic DB/Admin ===
MAUTIC_DB_PASS="ChangeMe!Strong2"

# === SMTP Relay (optional; if your host blocks outbound 25) ===
USE_SMTP_RELAY="no"   # yes|no
RELAY_HOST="email-smtp.us-east-1.amazonaws.com"
RELAY_PORT="587"
RELAY_USER="AKIA..."
RELAY_PASS="xxxxxxxxxxxxxxxx"

# === Mailcow API (for bulk script 70) ===
MAILCOW_BASE="https://${MAIL_DOMAIN}"
MAILCOW_API_KEY="PUT_YOUR_MAILCOW_API_KEY_HERE"   # create it in Mailcow UI
